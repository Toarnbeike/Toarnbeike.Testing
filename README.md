# Toarnbeike.Testing

`Toarnbeike.Testing` is a utility library for test projects.
Currently it only provides TestLoggers, implementations of `ILoggingFactory` and `ILogger` to assert logging output in tests.

> This library is currently intended for internal use and experimentation.